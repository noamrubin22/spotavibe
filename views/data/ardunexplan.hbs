<div class="content-arduino">

  <div class="header-background">
    <div class="header">
      <div class="user-info">
        <img src="{{user.userPhoto}}" alt="">
        <h4>{{user.userJson.display_name}}</h4>
      </div>
      <div class="title">
        <h1>Find your vibe</h1>
        <h5>Measure your heart rate or choose a BPM</h5>
      </div>
    </div>
  </div>

  <div class="arduino">
    <form action="/data/arduino" method="post" class="arduinoBPM">
      <h3>Explanation about Arduino</h3>
      {{!-- does not work yet :( i don't know why) --}}
      <h5>Download <a href="/public/javascripts/HeartPulseMeasurer_project2.ino" download>
          this Arduino script!</a>
      </h5>
      <h6>Once downloaded, you are ready to measure. Place your finger on the sensor in a quiet setting.</h6>
      <h6> Be patient as it takes some time for the Arduino to start measuring. The built-in LED light will blink when
        the heartpulse is detected.</h6>
      <h4>Choose a genre</h4>
      <select name="dropdown" id='dropdown'>
        {{!-- being populated via DOM manipulation --}}
      </select>
      <h4>Add a name to your playlist</h4>
      <p>(Perhaps how you feel?)</p>
      <input type="text" name="playlistName" id="playlistName" placeholder="name">
      <h4>Add the name of your Arduino port :</h4>
      <input type="text" name="arduinoPort" id="arduinoPort" value="/dev/cu.wchusbserial1410">
      {{#if errorMessage}}
      <div class="error-message">{{ errorMessage }}</div>
      {{/if}}
      <button type="submit" class="show-heart">Click to start</button>
    </form>
    <div class="lds-heart">
    </div>
  </div>
</div>

<script src="/javascripts/genres.js"></script>